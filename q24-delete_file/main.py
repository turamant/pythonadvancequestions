"""Для удаления файла в Python вы можете использовать модуль os, который предоставляет
функции для взаимодействия с операционной системой. Вот решение, которое демонстрирует,
как удалить файл с обработкой возможных ошибок:

import os

# Укажите путь к файлу, который вы хотите удалить
file_path = "путь/к/вашему/файлу.txt"

# Проверьте, существует ли файл, прежде чем пытаться его удалить
if os.path.exists(file_path):
    # Попытайтесь удалить файл
    try:
        os.remove(file_path)
        print(f"{file_path} был успешно удален.")
    except OSError as e:
        print(f"Ошибка при удалении {file_path}: {e}")
else:
    print(f"Файл {file_path} не существует.")
    
Обязательно замените "путь/к/вашему/файлу.txt" на фактический путь к файлу, который вы
хотите удалить. Этот код обеспечивает обработку ошибок, чтобы справиться со случаями,
когда файл может не существовать или возникают проблемы с удалением файла.

Удаление файлов - это распространенная задача управления файлами в Python, и это решение
предоставляет обработку ошибок для выполнения этой задачи."""

import os

# Укажите путь к файлу, который вы хотите удалить
file_path = "data.pkl"

# Проверьте, существует ли файл, прежде чем пытаться его удалить
if os.path.exists(file_path):
    # Попытайтесь удалить файл
    try:
        os.remove(file_path)
        print(f"{file_path} был успешно удален.")
    except OSError as e:
        print(f"Ошибка при удалении {file_path}: {e}")
else:
    print(f"Файл {file_path} не существует.")
    